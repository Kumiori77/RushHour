<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.RushHour"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Help" android:label="HELP"></activity>
        <activity android:name=".Setting" android:label="SETTING"></activity>
        <activity android:name=".ShowRecords" android:label="RECORDS"></activity>
        <activity android:name=".LevelSelect" android:label="SEELECT LEVEL"></activity>
        <activity android:name=".play.GameStage" android:label="PLAY"></activity>


        <service android:name=".sound.Bgm" android:enabled="true"/>
        <service android:name=".sound.SoundEffect" android:enabled="true"/>
    </application>

</manifest>